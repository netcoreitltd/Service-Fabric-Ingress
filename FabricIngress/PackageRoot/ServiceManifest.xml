<?xml version="1.0" encoding="utf-8"?>
<ServiceManifest Name="FabricIngressPkg"
                 Version="1.5.0"
                 xmlns="http://schemas.microsoft.com/2011/01/fabric"
                 xmlns:xsd="http://www.w3.org/2001/XMLSchema"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <ServiceTypes>
    <!-- This is the name of your ServiceType. 
         This name must match the string used in RegisterServiceType call in Program.cs. -->
    <StatelessServiceType ServiceTypeName="FabricIngressType" />
  </ServiceTypes>

  <!-- Code package is your service executable. -->
  <CodePackage Name="Code" Version="1.5.0">
    <EntryPoint>
      <ExeHost>
        <Program>FabricIngress.exe</Program>
        <WorkingFolder>CodePackage</WorkingFolder>
      </ExeHost>
    </EntryPoint>
    <EnvironmentVariables>
      <EnvironmentVariable Name="ASPNETCORE_ENVIRONMENT" Value=""/>
    </EnvironmentVariables>
  </CodePackage>

  <!-- Config package is the contents of the Config directory under PackageRoot that contains an 
       independently-updateable and versioned set of custom configuration settings for your service. -->
  <ConfigPackage Name="Config" Version="1.5.0" />

  <Resources>
    <Endpoints>
        <!-- <Endpoint Protocol="http" Name="ServiceEndpoint" Type="Input" Port="8259" /> -->
		<!-- This port is used for standard http traffic and ACME Challenge -->
		<Endpoint Name="HttpEndpoint" Protocol="http" Port="80" Type="Input" />
		<!-- This port is used for SSL/TLS traffic that will be managed by Kestrel + LettuceEncrypt -->
		<Endpoint Name="SslEndpoint" Protocol="http" Port="443" Type="Input" />		
    </Endpoints>
  </Resources>
</ServiceManifest>